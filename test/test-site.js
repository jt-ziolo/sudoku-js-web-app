import { asset } from 'chai'
import { initializeSudokuGrid } from '../src/index'

describe('construct sudoku grid', () => {
  describe('initializeSudokuGrid', () => {
    it('throws an error when the document object cannot be located')
    it('successfully adds sudoku grid square elements to the div')
    it('adds unfilled sudoku grid square elements')
  })
})
